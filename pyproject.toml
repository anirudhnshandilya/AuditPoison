[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "auditpoison"
version = "0.1.0"
description = "Day-1 pilot benchmark for adversarial evidence attacks on LLM cybersecurity auditors"
requires-python = ">=3.10"
dependencies = ["jsonschema>=4.18"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
